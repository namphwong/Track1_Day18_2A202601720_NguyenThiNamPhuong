# Human–AI Decision Table — Day 18 (CP3: Human Control)

**Case:** C — AI Support Radar (VLearn)
**Trạng thái:** Option A, Option B và Option C đã implement thành prototype ([prototype-link.md](prototype-link.md)). Cả ba đã tự kiểm bằng smoke test nội bộ (jsdom); **chưa có tester ngoài nhóm thao tác**.

| Tiêu chí | Option A — Coach Query | Option B — AI Review Queue | Option C — Proactive Agent |
| --- | --- | --- | --- |
| **Trạng thái** | ✅ Đã implement (prototype) | ✅ Đã implement (prototype) | ✅ Đã implement (prototype) |
| **Expectation** — coach biết AI sắp làm gì trước khi AI chạy | Banner đầu trang nói rõ AI chưa phân tích/tổng hợp evidence gì cho tới khi coach bấm yêu cầu; nút "Kiểm tra nhóm đang chậm" chỉ bật sau khi chọn checkpoint. Checkpoint list vẫn hiển thị sẵn số liệu tổng hợp kiểu-VLAB (ví dụ "7/10 nhóm đã qua") — đây là dữ liệu có sẵn từ trước, không phải AI mới phân tích. | Banner nêu rõ: "AI đã tự động gom tín hiệu và tạo review queue... AI không liên hệ learner cho tới khi bạn approve." Coach biết ngay khi mở tab rằng queue là do AI tự tạo, không phải coach yêu cầu. | Banner nói thẳng: AI có thể **đã Act hoặc đã Ask trước khi coach mở tab**. Màn hình đầu tiên (Common Context) không phải một danh sách chờ coach yêu cầu, mà là **policy đang áp dụng + audit log** những gì AI đã tự làm — coach biết ngay AI không "trắng tay" chờ lệnh như Option A. |
| **Role & Agency** — User làm gì / AI làm gì | User: chọn phạm vi, bấm yêu cầu, đọc evidence, quyết định. AI: chỉ phân tích khi được lệnh, không tự xếp hạng toàn lớp. | User: mở case, đọc evidence + uncertainty, chỉnh priority, yêu cầu thêm evidence, approve/dismiss. AI: tự gom & xếp hạng, giải thích lý do, không tự hành động với learner. | User: đọc/sửa policy, xem audit log, mở từng case để undo hoặc quyết định, tắt theo dõi theo nhóm, tạm dừng policy toàn cục. AI: tự động gửi check-in rủi ro thấp cho case khớp policy (Nhóm 07), tự quyết định KHÔNG trả lời và chuyển thẳng coach khi learner đã chủ động yêu cầu (Nhóm 09), và tự quyết định không hành động khi tín hiệu bình thường (Nhóm 03). |
| **AI Act / Ask / Don't Act** | **Don't Act** cho tới khi coach yêu cầu; sau đó chỉ **Act** ở mức phân tích/tổng hợp, không hành động với learner. | **Act** để tạo và xếp review queue (không cần lệnh coach); **Ask** (chờ approve) trước khi tạo bước hỗ trợ hoặc chạm tới learner. | Cả ba nhánh cùng xuất hiện trong một prototype, trên cùng bộ dữ liệu: **Act** — Nhóm 07 được AI tự gửi một check-in trung lập, rủi ro thấp, có thể thu hồi, vì evidence khớp policy tự-hành-động nhưng chưa có yêu cầu trực tiếp nào từ learner. **Ask** — Nhóm 09 đã tự gửi yêu cầu trợ giúp; AI coi yêu cầu trực tiếp từ người thật là "ảnh hưởng lớn" nên **không tự soạn trả lời**, chuyển thẳng case cho coach. **Don't Act** — Nhóm 03 có tín hiệu bình thường nên AI không tạo hành động nào, chỉ ghi log theo dõi. |
| **Ai giữ quyền quyết định cuối** | Coach — 100%, mọi bước. | Coach — mọi hành động chạm tới learner cần approve; AI chỉ tự chủ ở việc tạo/xếp queue nội bộ. | Coach — AI có thể tự gửi một câu hỏi trung lập ở mức rủi ro thấp, nhưng **không được tự đóng case của một learner cụ thể**; chỉ coach mới đóng case (chọn Hỗ trợ ngay/Lên lịch/Bỏ qua), kể cả khi learner đã tự phản hồi "không cần giúp". |
| **Evidence & Uncertainty** — AI dựa vào tín hiệu nào, giới hạn ra sao | Evidence panel liệt kê 4 tín hiệu (thời gian dừng, yêu cầu trợ giúp, số lần mở tài liệu, lịch sử hỗ trợ), mỗi tín hiệu có ghi chú giới hạn riêng. Khối "Mức độ chắc chắn" nói rõ đây là tương quan, không phải bằng chứng chắc chắn mắc kẹt. | Cùng 4 tín hiệu + cùng khối uncertainty như Option A. Thêm: mỗi case trong queue có dòng lý do AI đề xuất priority (không chỉ số điểm), và nút "+ Yêu cầu thêm evidence" mở dữ liệu bổ sung kèm dòng "Giới hạn dữ liệu" tường minh. | Cùng evidence panel + uncertainty box như A/B. Thêm khối "Hành động AI" riêng cho từng case: với Nhóm 07 hiển thị đúng nội dung check-in đã gửi, độ tin cậy ("Trung bình"), mức rủi ro và quy tắc policy đã khớp — không giấu logic đằng sau hành động tự động. |
| **Không trình bày "chậm checkpoint" như bằng chứng chắc chắn** | Đạt — mọi ghi chú tín hiệu đều dùng ngôn ngữ "có thể", "chưa chắc chắn", không kết luận thay coach. | Đạt — tương tự A; priority AI đề xuất luôn kèm lý do và giới hạn, không chỉ một con số. | Đạt — check-in gửi cho Nhóm 07 dùng câu hỏi trung lập ("có cần gợi ý không"), không khẳng định nhóm đang gặp lỗi; khối "Hành động AI" ghi rõ độ tin cậy chỉ ở mức Trung bình, không phải kết luận chắc chắn. |
| **Control & Recovery** | Approve/Reject: coach chọn Hỗ trợ ngay / Lên lịch / Bỏ qua. Edit: chọn lại nhóm/checkpoint khác. Back: nút quay lại checkpoint list, quay lại evidence. Reset: nút reset toàn cục đưa cả A và B về common context. | Approve/Reject: Hỗ trợ ngay / Lên lịch / Dismiss. Edit: dropdown chỉnh priority trước khi quyết định. Back: quay lại queue, quay lại case. Reset: nút reset toàn cục. | Undo: nút "Thu hồi check-in" cho Nhóm 07 trước khi có phản hồi. Stop: toggle "Tạm dừng toàn bộ hành động tự động của AI" ở màn hình policy. Opt-out: toggle "Tắt hành động/theo dõi chủ động" theo từng nhóm. Audit log: mọi hành động AI và quyết định coach đều được ghi lại có thời gian, xem lại được ở màn hình Common Context. Back/Reset: quay lại case, quay lại log, và reset toàn cục như A/B. |
| **Nếu AI sai, coach vẫn tiếp tục task ban đầu** | Có — mọi trạng thái đều có đường quay lại, không có ngõ cụt; chọn "Bỏ qua — chưa đủ evidence" vẫn cho phép quay lại chọn checkpoint khác. | Có — dismiss một case không khóa các case khác trong queue; reset đưa toàn bộ về ban đầu. | Có — nếu check-in tự động là sai lầm (ví dụ nhóm không hề gặp vấn đề), coach undo được trước khi learner phản hồi, hoặc vẫn override được sau khi learner đã phản hồi; không có bước nào của AI khoá coach lại một hướng duy nhất. |

## Ghi chú Gate 3 self-check (áp dụng cho cả A, B, C — đều đã implement)

- [x] A, B và C thể hiện rõ AI Act/Ask/Don't Act khác nhau, không chỉ khác wording — C còn thể hiện cả ba trạng thái này trong cùng một prototype trên ba case khác nhau.
- [x] Evidence luôn đi kèm giới hạn/uncertainty, không trình bày như kết luận chắc chắn.
- [x] Coach có đường approve, reject/dismiss, edit (priority ở B, chọn lại phạm vi ở A, undo/opt-out ở C), back và reset ở cả ba option.
- [x] AI không tự đóng case chạm tới learner cụ thể ở cả ba option — kể cả ở C, nơi AI được phép tự gửi một câu hỏi nhưng không được tự kết luận case.
- [x] Option C có guardrail rõ ràng cho Act (rủi ro thấp, có thể hoàn tác) và Ask (evidence mâu thuẫn hoặc ảnh hưởng lớn — ở đây là yêu cầu trực tiếp từ learner), đúng như thiết kế ở CP2.

**Kết luận:** Gate 3 tự kiểm được cho cả Option A, B và C dựa trên prototype đã chạy và smoke test nội bộ. Đây vẫn là tự kiểm của nhóm, chưa có tester ngoài nhóm xác nhận — thuộc phạm vi Gate 4.
